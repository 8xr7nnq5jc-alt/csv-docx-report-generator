{
  "name": "csv-docx-report-generator",
  "version": "1.0.0",
  "description": "A small Node.js web app that turns CSV uploads into downloadable Word reports with login, report type selection and activity logs.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "docx": "^9.5.1",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}

